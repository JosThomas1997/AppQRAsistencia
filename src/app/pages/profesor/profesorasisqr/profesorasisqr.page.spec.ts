import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ProfesorasisqrPage } from './profesorasisqr.page';

describe('ProfesorasisqrPage', () => {
  let component: ProfesorasisqrPage;
  let fixture: ComponentFixture<ProfesorasisqrPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ProfesorasisqrPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
